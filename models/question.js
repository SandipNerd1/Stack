class Question {
  constructor(id, title, description, codeText) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.codeText = codeText;
  }
}

export default Question;
